/*
do
{
		循环体;
}while(条件表达式);

工作原理：
先执行循环体，然后再进行条件判断，条件为真，继续执行循环体，条件为假跳出循环


注意事项：
1,循环之前一定要初始化循环变量
2,在循环体内一定要改变循环变量的值
3,条件表达式的后面一定要有分号
*/

#include <stdio.h>
int main()
{
		int i=1;
		do
		{
				printf("hehe\n");
			//	i ++;
		}while(i<=3);
        printf("i=%d\n",i);
		return 0;
}
